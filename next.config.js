/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['media0.giphy.com','file-examples.com','localhost']
},

}

module.exports = nextConfig
